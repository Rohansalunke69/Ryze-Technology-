{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && git add \"$f\" && git commit -m \"auto: update $(basename \\\"$f\\\")\" 2>/dev/null || true; }",
            "statusMessage": "Auto-committing changes..."
          }
        ]
      }
    ]
  }
}
